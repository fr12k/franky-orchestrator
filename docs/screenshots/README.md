Replace these placeholder files with real screenshots:

- `dashboard.png` — the dashboard grid view showing connected agent cards
- `timeline.png`  — the waterfall timeline view with live events

Recommended: capture at 2x resolution, crop to the browser viewport (excluding browser chrome).
